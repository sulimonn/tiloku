// project import
//import pages from './pages';
import { additionalBooks } from "./additionalBooks";
import classes from "./classes";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [classes, additionalBooks],
};

export default menuItems;
